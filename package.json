{
  "name": "biliarchiverbot-tgcloud",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "check": "node scripts/validate-project.mjs",
    "status": "tgcloud status",
    "diff": "tgcloud diff",
    "push": "tgcloud push",
    "migrate": "tgcloud migrate",
    "webhook:sync": "tgcloud webhook sync",
    "deploy": "npm run check && npm run push && npm run migrate"
  },
  "devDependencies": {
    "@tgcloud/cli": "0.1.2"
  }
}
